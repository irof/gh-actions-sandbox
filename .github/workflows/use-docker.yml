name: DockerHubのイメージを使う

on:
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo hello
          ls -al
      - name: run alpine
        uses: docker://alpine
        with:
          entrypoint: /bin/ls
          args: -al
