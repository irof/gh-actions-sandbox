name: こんにちは

on:
  push

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print a greeting
        run: |
          echo hello world.
      - name: curl
        run: curl -v
      - name: jq
        run: jq -v

